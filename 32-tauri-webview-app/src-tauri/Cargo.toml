[package]
name = "tauri_webview_app"
version = "1.0.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
tauri = { version = "1", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
