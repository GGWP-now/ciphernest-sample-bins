{
  "name": "jsts-cli",
  "version": "1.0.0",
  "description": "JS/TS CLI victim -- prime sieve + Fibonacci",
  "comment": "CommonJS output so the entry works as a Node SEA (single executable) bundle",
  "type": "commonjs",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.4.0"
  }
}
