# Win32 Delphi i386 Victim

Builds a 32-bit Windows Delphi-style console executable using `dcc32` when
available, with Free Pascal as a fallback.

```powershell
.\build.ps1 -OutDir .\bin -SkipIfUnavailable
```
