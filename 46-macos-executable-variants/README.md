# macOS Executable Variants

Builds Mach-O command-line executables for i386, x86_64, and arm64 when the
active Xcode toolchain still supports the requested architecture.

```sh
sh build.sh build
```
